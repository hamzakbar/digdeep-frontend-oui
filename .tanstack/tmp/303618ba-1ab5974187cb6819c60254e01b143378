import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/session/$sessionId/files')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/session/$sessionId/files"!</div>
}
